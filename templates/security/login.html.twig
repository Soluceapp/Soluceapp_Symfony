{% extends 'base.html.twig' %} 

{% block title %}Mooc.soluceapp.com{% endblock %}

{% block body %}

<form method="post">
    {% if error %}
        <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'fsecurity') }}</div>
    {% endif %}

    {% if app.user %}
        <div class="mb-3">
            You are logged in as {{ app.user.userIdentifier }}, <a href="{{ path('app_logout') }}">Se déconnecter</a>
        </div>
    {% endif %}
<div style="margin:20px"><div class="style25">
    <h4 >Connectez-vous</h4>

    <label for="inputEmail">Identifiant (mail) &nbsp;</label>
    <input type="email" value="{{ last_username }}" name="email" id="inputEmail"  autocomplete="email" required autofocus>
    <br><br>
    <label for="inputPassword">Mot de passe &nbsp;</label>
    <input type="password" name="password" id="inputPassword"  autocomplete="current-password" required>
    <br><br>
    <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">
    <button class=" btn btn-primary" style="margin:5px" type="submit">Valider</button>
    <a href="{{ path('forgottent_password')}}" ><input style="margin:5px" class="btn btn-primary" type="button" value="Mot de passe oublié"></a>
    <a href="{{ path('app_register')}}" ><input style="margin:5px" class="btn btn-primary" type="button" value="Inscription"></a>
    <br><br>
</div></div>
</form>
{% endblock %}
